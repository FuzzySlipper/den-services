# Handoff service configuration.
# Copy to /data/services/handoff/config/config.yaml for deployment.

bind_addr: "127.0.0.1:8099"
database_url_env: "DEN_HANDOFF_DATABASE_URL"
service_token_env: "DEN_HANDOFF_SERVICE_TOKEN"

http:
  read_header_timeout: "5s"
