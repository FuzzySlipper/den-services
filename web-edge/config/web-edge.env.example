# Browser traffic carries no service credential. The edge replaces any inbound
# Authorization header with this dedicated Gateway caller token.
DEN_WEB_EDGE_GATEWAY_TOKEN=replace-with-gateway-web-caller-token

# Optional. The systemd template defaults this to the installed config path.
# WEB_EDGE_CONFIG_PATH=/etc/den-services/web-edge.yaml
